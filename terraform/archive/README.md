Legacy Terraform root directories live here.

These stacks have been superseded by the active layout under:

- `terraform/modules/`
- `terraform/stacks/`

Do not run `terraform apply` from archived roots unless you are doing explicit historical recovery work and have verified state ownership first.
