---
# Homepage Dashboard Deployment using Reusable Helm Template

- name: Deploy Homepage Dashboard using reusable Helm template
  include_tasks: ../shared/helm_app_deployment.yml
  vars:
    app_name: "homepage"
    helm_config:
      repo_name: "{{ app_definitions.homepage.helm.repo_name }}"
      repo_url: "{{ app_definitions.homepage.helm.repo_url }}"
      chart_name: "{{ app_definitions.homepage.helm.chart_name }}"
      release_name: "{{ app_definitions.homepage.helm.release_name }}"
      namespace: "{{ app_definitions.homepage.helm.namespace }}"
      chart_version: "{{ app_definitions.homepage.helm.chart_version | default(omit) }}"
      values_template: "homepage/helm-values.yaml.j2"
    app_config:
      force_redeploy: "{{ 'homepage' in force_redeploy_apps }}"
      service_name: "{{ app_definitions.homepage.service_name }}"
      app_url: "{{ app_definitions.homepage.app_url }}"
      service_port: "{{ app_definitions.homepage.service_port }}"
      storage: "{{ app_definitions.homepage.storage }}"
      pod_selector: "app.kubernetes.io/name=homepage"  # Homepage-specific selector