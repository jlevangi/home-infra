---
# K3s Apps Role Defaults
# These provide fallback values when not explicitly set in group_vars

# Default application configurations (fallback only)
# Primary values should be set in group_vars for each environment

# Note: The following variables are managed in group_vars for each environment:
# - deploy_bookstack/homepage/vaultwarden: Controls which apps to deploy
# - bookstack_force_redeploy/homepage_force_redeploy/vaultwarden_force_redeploy: Force redeployment controls
# - All storage and configuration settings