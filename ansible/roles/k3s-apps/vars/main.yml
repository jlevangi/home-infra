---
# Application configuration has been moved to nested structure in group_vars
# 
# All application settings are now organized under the 'applications' key:
#
# applications:
#   bookstack:
#     app_url: "https://bookstack.{{ cluster_tld }}"
#     service_name: "bookstack"
#     service_port: 8080
#     storage: { ... }
#     secrets: { ... }
#
#   homepage:
#     app_url: "https://homepage.{{ cluster_tld }}"
#     service_name: "homepage"  
#     service_port: 3000
#     storage: { ... }
#
#   vaultwarden:
#     app_url: "https://vw.{{ cluster_tld }}"
#     service_name: "vaultwarden"
#     service_port: 80
#     storage: { ... }
#     secrets: { ... }
#
# See group_vars/k3s_cluster.yml and group_vars/k3s_cluster_test.yml for actual configuration.