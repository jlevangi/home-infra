---
# BookStack Secrets - Generated by Ansible
apiVersion: v1
kind: Secret
metadata:
  name: bookstack-secrets
  namespace: bookstack
type: Opaque
data:
  # Base64 encoded database password
  DB_PASS: {{ bookstack_db_password | b64encode }}
  
  # Base64 encoded MariaDB root password  
  MYSQL_ROOT_PASSWORD: {{ bookstack_mysql_root_password | b64encode }}
  
  # Base64 encoded MariaDB user password (same as DB_PASS)
  MYSQL_PASSWORD: {{ bookstack_db_password | b64encode }}
  
  # Base64 encoded Laravel application key
  APP_KEY: {{ bookstack_app_key | b64encode }}
