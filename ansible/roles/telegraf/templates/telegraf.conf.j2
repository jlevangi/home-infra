# Telegraf Configuration for K3s Nodes
# Generated by Ansible

[global_tags]
  environment = "{{ telegraf_environment }}"
  cluster = "{{ telegraf_cluster_name }}"
  node_type = "{% if inventory_hostname in master_group %}master{% else %}worker{% endif %}"
  node_name = "{{ inventory_hostname }}"

[agent]
  interval = "{{ telegraf_interval }}"
  round_interval = true
  metric_batch_size = 1000
  metric_buffer_limit = 10000
  collection_jitter = "0s"
  flush_interval = "10s"
  flush_jitter = "0s"
  precision = ""
  hostname = "{{ inventory_hostname }}"
  omit_hostname = false

# System Metrics
[[inputs.system]]

[[inputs.cpu]]
  percpu = true
  totalcpu = true
  collect_cpu_time = false
  report_active = false

[[inputs.disk]]
  ignore_fs = ["tmpfs", "devtmpfs", "devfs", "overlay", "squashfs"]
  mount_points = ["/", "/mnt/k3s-storage"]

[[inputs.diskio]]

[[inputs.mem]]

[[inputs.swap]]

[[inputs.processes]]

[[inputs.nstat]]

[[inputs.net]]
  interfaces = ["eth*", "ens*", "flannel*", "cni*"]

# Kubernetes/K3s specific monitoring
{% if k3s_monitoring_token is defined and k3s_monitoring_token != "" %}
[[inputs.kubernetes]]
  url = "https://{{ ansible_default_ipv4.address }}:6443"
  bearer_token = "{{ k3s_monitoring_token }}"
  insecure_skip_verify = true
  response_timeout = "5s"
{% endif %}

# Monitor containerd (K3s container runtime)
[[inputs.docker]]
  endpoint = "unix:///run/containerd/containerd.sock"
  timeout = "5s"

# Monitor systemd services
[[inputs.systemd_units]]
  unittype = "service"
  pattern = "k3s*"

# Monitor kernel metrics
[[inputs.kernel]]

# Monitor temperature (if available)
[[inputs.temp]]

# InfluxDB Output
[[outputs.influxdb]]
  urls = ["{{ telegraf_influxdb_url }}"]
  database = "{{ telegraf_database }}"
  username = "{{ telegraf_username }}"
  password = "{{ telegraf_password }}"
  retention_policy = "{{ telegraf_retention_policy }}"
  write_consistency = "any"
  timeout = "5s"