apiVersion: v1
kind: Service
metadata:
  name: raft
  namespace: raft
spec:
  type: ClusterIP
  ports:
    - name: game
      port: 27015
      targetPort: 27015
      protocol: UDP
  selector:
    app: raft
