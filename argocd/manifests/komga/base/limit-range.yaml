apiVersion: v1
kind: LimitRange
metadata:
  name: komga-storage-limits
  namespace: komga
spec:
  limits:
    - type: PersistentVolumeClaim
      max:
        storage: "10Gi"
      min:
        storage: "1Gi"
      default:
        storage: "10Gi"
      defaultRequest:
        storage: "10Gi"
