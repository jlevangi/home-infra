apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: bookstack-db-data-pvc
  namespace: bookstack
  labels:
    app: bookstack
    component: database
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 500Mi
  storageClassName: longhorn
