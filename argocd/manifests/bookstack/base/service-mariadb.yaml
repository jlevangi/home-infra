apiVersion: v1
kind: Service
metadata:
  name: bookstack-mariadb
  namespace: bookstack
spec:
  selector:
    app: bookstack-mariadb
  ports:
    - port: 3306
      targetPort: 3306
  type: ClusterIP
