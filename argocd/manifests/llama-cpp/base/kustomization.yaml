apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - pv.yaml
  - pvc.yaml
  - deployment.yaml
  - service.yaml
  - service-embeddings.yaml
  - ingress.yaml
configMapGenerator:
  - name: llama-swap-config
    namespace: llama-cpp
    files:
      - config.yaml=config.yaml
    options:
      labels:
        app: llama-cpp
  - name: llama-metrics-sidecar
    namespace: llama-cpp
    files:
      - sidecar-exporter.py=sidecar-exporter.py
    options:
      labels:
        app: llama-cpp
