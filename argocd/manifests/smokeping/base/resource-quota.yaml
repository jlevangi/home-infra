apiVersion: v1
kind: ResourceQuota
metadata:
  name: smokeping-storage-quota
  namespace: smokeping
spec:
  hard:
    requests.storage: "1Gi"
    persistentvolumeclaims: "1"
    longhorn.io/requests.storage: "1Gi"
