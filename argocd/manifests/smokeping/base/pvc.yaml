apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: smokeping-data-pvc
  namespace: smokeping
  labels:
    app: smokeping
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: longhorn
