apiVersion: v1
kind: Service
metadata:
  name: paperless-mariadb
  namespace: paperless
spec:
  selector:
    app: paperless-mariadb
  ports:
    - port: 3306
      targetPort: 3306
  type: ClusterIP
