apiVersion: v1
kind: ResourceQuota
metadata:
  name: actual-budget-storage-quota
  namespace: actual-budget
spec:
  hard:
    requests.storage: "1Gi"
    persistentvolumeclaims: "1"
    longhorn.io/requests.storage: "1Gi"
