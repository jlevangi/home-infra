apiVersion: batch/v1
kind: CronJob
metadata:
  name: vault-auto-recovery
  namespace: vault-raft
  labels:
    app.kubernetes.io/name: vault-auto-recovery
    app.kubernetes.io/component: maintenance
spec:
  schedule: "*/5 * * * *"
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 5
  jobTemplate:
    metadata:
      labels:
        app.kubernetes.io/name: vault-auto-recovery
        app.kubernetes.io/component: maintenance
    spec:
      backoffLimit: 0
      template:
        metadata:
          labels:
            app.kubernetes.io/name: vault-auto-recovery
            app.kubernetes.io/component: maintenance
        spec:
          serviceAccountName: vault-auto-recovery
          restartPolicy: Never
          containers:
            - name: recover
              # Bitnami publishes date/build-specific immutable tags, but not
              # every Kubernetes patch version as a plain semver tag. `latest`
              # was smoke-tested in prod and currently provides kubectl v1.36.1
              # with /bin/sh, which this ConfigMap-mounted script requires.
              image: bitnami/kubectl:latest
              imagePullPolicy: IfNotPresent
              command:
                - /bin/sh
                - /scripts/recover.sh
              env:
                - name: VAULT_NAMESPACE
                  value: vault-raft
                - name: VAULT_ACTIVE_ADDR
                  value: http://vault-raft-active.vault-raft.svc:8200
              volumeMounts:
                - name: scripts
                  mountPath: /scripts
                  readOnly: true
          volumes:
            - name: scripts
              configMap:
                name: vault-auto-recovery
                defaultMode: 0555
