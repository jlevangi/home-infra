apiVersion: v1
kind: ResourceQuota
metadata:
  name: netbootxyz-storage-quota
  namespace: netbootxyz
spec:
  hard:
    requests.storage: "1Gi"
    persistentvolumeclaims: "1"
    longhorn.io/requests.storage: "1Gi"
