apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: swiparr-oauth2-proxy-ingress
  namespace: swiparr
  annotations:
    traefik.ingress.kubernetes.io/router.entrypoints: web,websecure
spec:
  ingressClassName: traefik
  rules:
    - host: swipe.levangie.dev
      http:
        paths:
          - path: /oauth2
            pathType: Prefix
            backend:
              service:
                name: oauth2-proxy
                port:
                  number: 4180
