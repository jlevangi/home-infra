apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: swiparr-ingress
  namespace: swiparr
  annotations:
    traefik.ingress.kubernetes.io/router.entrypoints: web,websecure
    traefik.ingress.kubernetes.io/router.middlewares: swiparr-swiparr-oauth2-errors@kubernetescrd,swiparr-swiparr-oauth2-forwardauth@kubernetescrd
spec:
  ingressClassName: traefik
  rules:
    - host: swipe.levangie.dev
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: swiparr
                port:
                  number: 4321
