apiVersion: v1
kind: Service
metadata:
  labels:
    environment: staging
  name: gk-postgrest
spec:
  ports:
  - name: http
    port: 3000
    targetPort: http
  selector:
    app: gk-postgrest
    environment: staging
