apiVersion: v1
kind: Service
metadata:
  labels:
    environment: staging
  name: gk-functions
spec:
  ports:
  - name: http
    port: 9000
    targetPort: http
  selector:
    app: gk-functions
    environment: staging
