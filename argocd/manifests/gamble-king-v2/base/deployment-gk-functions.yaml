apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: gk-functions
    environment: dev
  name: gk-functions
spec:
  replicas: 1
  selector:
    matchLabels:
      app: gk-functions
      environment: dev
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: gk-functions
        environment: dev
    spec:
      containers:
      - env:
        - name: SUPABASE_URL
          value: https://dev.gambleking.app
        - name: APP_ORIGIN
          value: https://dev.gambleking.app
        - name: SUPABASE_ANON_KEY
          valueFrom:
            secretKeyRef:
              key: ANON_KEY
              name: gamble-king-v2-secrets
        - name: SUPABASE_SERVICE_ROLE_KEY
          valueFrom:
            secretKeyRef:
              key: SERVICE_ROLE_KEY
              name: gamble-king-v2-secrets
        # Stripe values are optional until test-mode credentials exist. The
        # functions fail closed with HTTP 503 while these keys are absent.
        - name: STRIPE_SECRET_KEY
          valueFrom:
            secretKeyRef:
              key: STRIPE_SECRET_KEY
              name: gamble-king-v2-secrets
              optional: true
        - name: STRIPE_PRICE_ID
          valueFrom:
            secretKeyRef:
              key: STRIPE_PRICE_ID
              name: gamble-king-v2-secrets
              optional: true
        - name: STRIPE_WEBHOOK_SECRET
          valueFrom:
            secretKeyRef:
              key: STRIPE_WEBHOOK_SECRET
              name: gamble-king-v2-secrets
              optional: true
        image: ghcr.io/jlevangi/gamble-king:functions-a9a0c36a99255a268e6afe17420aa535fa7668e9@sha256:f6f1a61ff41d43db424e3a2797a17d6e29f2a6eeb5aa214d27bf79fdeb794877
        imagePullPolicy: IfNotPresent
        name: functions
        ports:
        - containerPort: 9000
          name: http
        readinessProbe:
          failureThreshold: 6
          initialDelaySeconds: 5
          periodSeconds: 10
          tcpSocket:
            port: http
        resources:
          limits:
            cpu: 500m
            memory: 512Mi
          requests:
            cpu: 50m
            memory: 128Mi
        securityContext:
          allowPrivilegeEscalation: false
          capabilities:
            drop:
            - ALL
      imagePullSecrets:
      - name: gamble-king-v2-ghcr
