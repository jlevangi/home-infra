apiVersion: v1
kind: Service
metadata:
  labels:
    environment: staging
  name: gk-postgres
spec:
  ports:
  - name: postgres
    port: 5432
    targetPort: postgres
  selector:
    app: gk-postgres
    environment: staging
