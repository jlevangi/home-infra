apiVersion: v1
kind: Service
metadata:
  name: headroom
  namespace: 9router
  labels:
    app: headroom
    app.kubernetes.io/name: headroom
spec:
  type: ClusterIP
  selector:
    app: headroom
  ports:
    - name: http
      port: 8787
      targetPort: 8787
      protocol: TCP
