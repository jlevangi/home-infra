apiVersion: v1
kind: LimitRange
metadata:
  name: 9router-storage-limits
  namespace: 9router
spec:
  limits:
    - type: PersistentVolumeClaim
      max:
        storage: "1Gi"
      min:
        storage: "100Mi"
      default:
        storage: "1Gi"
      defaultRequest:
        storage: "1Gi"
