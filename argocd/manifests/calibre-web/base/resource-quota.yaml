apiVersion: v1
kind: ResourceQuota
metadata:
  name: calibre-web-storage-quota
  namespace: calibre-web
spec:
  hard:
    requests.storage: "10Gi"
    persistentvolumeclaims: "1"
    longhorn.io/requests.storage: "10Gi"
