apiVersion: v1
kind: ResourceQuota
metadata:
  name: tasksmd-storage-quota
  namespace: tasksmd
spec:
  hard:
    requests.storage: "250Mi"
    persistentvolumeclaims: "1"
    longhorn.io/requests.storage: "250Mi"
