apiVersion: v1
kind: ConfigMap
metadata:
  name: crowdsec-lapi-config
  namespace: crowdsec
data:
  config.yaml: |
    db_config:
      type: sqlite
      db_path: /var/lib/crowdsec/data/crowdsec.db
    api:
      server:
        listen_uri: 0.0.0.0:8080
        online_client: null
