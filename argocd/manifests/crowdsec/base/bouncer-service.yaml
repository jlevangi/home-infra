apiVersion: v1
kind: Service
metadata:
  name: crowdsec-bouncer
  namespace: crowdsec
  labels:
    app: crowdsec-bouncer
    app.kubernetes.io/name: crowdsec-bouncer
spec:
  type: ClusterIP
  selector:
    app: crowdsec-bouncer
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
