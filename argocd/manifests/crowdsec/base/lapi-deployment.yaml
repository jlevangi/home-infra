apiVersion: apps/v1
kind: Deployment
metadata:
  name: crowdsec-lapi
  namespace: crowdsec
  labels:
    app: crowdsec-lapi
    app.kubernetes.io/name: crowdsec-lapi
spec:
  replicas: 1
  selector:
    matchLabels:
      app: crowdsec-lapi
  template:
    metadata:
      labels:
        app: crowdsec-lapi
    spec:
      serviceAccountName: crowdsec-agent
      initContainers:
        - name: init-config
          image: busybox:1.36
          command:
            - /bin/sh
            - -c
            - |
              cp /tmp/config/config.yaml /etc/crowdsec/config.yaml
              chmod 644 /etc/crowdsec/config.yaml
          volumeMounts:
            - name: configmap
              mountPath: /tmp/config
            - name: config
              mountPath: /etc/crowdsec
      containers:
        - name: crowdsec-lapi
          image: crowdsecurity/crowdsec:latest
          imagePullPolicy: IfNotPresent
          env:
            - name: CONFIG_PATH
              value: /etc/crowdsec/config.yaml
            - name: DATA_PATH
              value: /var/lib/crowdsec/data
            - name: GID
              value: "1000"
            - name: UID
              value: "1000"
          ports:
            - name: lapi
              containerPort: 8080
              protocol: TCP
          volumeMounts:
            - name: config
              mountPath: /etc/crowdsec
            - name: data
              mountPath: /var/lib/crowdsec/data
          livenessProbe:
            httpGet:
              path: /health
              port: lapi
            initialDelaySeconds: 30
            periodSeconds: 15
          readinessProbe:
            httpGet:
              path: /health
              port: lapi
            initialDelaySeconds: 10
            periodSeconds: 5
          resources:
            requests:
              memory: 128Mi
              cpu: 100m
            limits:
              memory: 512Mi
              cpu: 500m
      volumes:
        - name: configmap
          configMap:
            name: crowdsec-lapi-config
        - name: config
          emptyDir: {}
        - name: data
          persistentVolumeClaim:
            claimName: crowdsec-lapi-data
