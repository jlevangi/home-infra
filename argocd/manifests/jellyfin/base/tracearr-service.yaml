apiVersion: v1
kind: Service
metadata:
  name: tracearr
  namespace: jellyfin
spec:
  selector:
    app: tracearr
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
      name: http
  type: ClusterIP
