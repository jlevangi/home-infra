apiVersion: v1
kind: Service
metadata:
  name: tunarr
  namespace: jellyfin
spec:
  type: ClusterIP
  selector:
    app: tunarr
  ports:
    - name: http
      port: 8000
      targetPort: 8000
      protocol: TCP
