apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: tunarr-config-pvc
  namespace: jellyfin
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn-redundant
  resources:
    requests:
      storage: 10Gi
