apiVersion: v1
kind: ResourceQuota
metadata:
  name: pocketid-storage-quota
  namespace: pocketid
spec:
  hard:
    requests.storage: "10Gi"
    persistentvolumeclaims: "20"
    longhorn.io/requests.storage: "10Gi"
