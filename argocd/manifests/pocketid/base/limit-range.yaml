apiVersion: v1
kind: LimitRange
metadata:
  name: pocketid-storage-limits
  namespace: pocketid
spec:
  limits:
    - type: PersistentVolumeClaim
      max:
        storage: "5Gi"
      min:
        storage: "100Mi"
      default:
        storage: "1Gi"
      defaultRequest:
        storage: "1Gi"
