apiVersion: v1
kind: Service
metadata:
  name: omniroute
  namespace: omniroute
  labels:
    app: omniroute
    app.kubernetes.io/name: omniroute
spec:
  type: ClusterIP
  selector:
    app: omniroute
  ports:
    - name: http
      port: 20128
      targetPort: 20128
      protocol: TCP
