apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: wallos-data-pvc
  namespace: wallos
  labels:
    recurring-job-group.longhorn.io/daily: "enabled"
    recurring-job-group.longhorn.io/weekly: "enabled"
    app: wallos
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: longhorn
