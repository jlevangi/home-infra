apiVersion: autoscaling.k8s.io/v1
kind: VerticalPodAutoscaler
metadata:
  name: wallos
  namespace: wallos
spec:
  targetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: wallos
  updatePolicy:
    updateMode: "Off"
