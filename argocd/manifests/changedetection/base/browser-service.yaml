apiVersion: v1
kind: Service
metadata:
  name: browser-chrome
  namespace: changedetection
  labels:
    app: browser-chrome
spec:
  type: ClusterIP
  ports:
    - port: 4444
      targetPort: 4444
      protocol: TCP
  selector:
    app: browser-chrome
