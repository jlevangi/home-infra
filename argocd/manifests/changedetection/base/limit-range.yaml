apiVersion: v1
kind: LimitRange
metadata:
  name: changedetection-storage-limits
  namespace: changedetection
spec:
  limits:
    - type: PersistentVolumeClaim
      max:
        storage: "500Mi"
      min:
        storage: "100Mi"
      default:
        storage: "500Mi"
      defaultRequest:
        storage: "500Mi"
