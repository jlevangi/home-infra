apiVersion: v1
kind: Service
metadata:
  name: boxbox
  namespace: boxbox
  labels:
    app.kubernetes.io/name: boxbox
spec:
  selector:
    app.kubernetes.io/name: boxbox
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
