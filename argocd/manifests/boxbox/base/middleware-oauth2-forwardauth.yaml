apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: boxbox-oauth2-forwardauth
  namespace: boxbox
spec:
  forwardAuth:
    address: http://10.43.210.90:4180/oauth2/auth
    trustForwardHeader: true
    authResponseHeaders:
      - X-Auth-Request-User
      - X-Auth-Request-Email
      - X-Auth-Request-Preferred-Username
      - Authorization
