apiVersion: v1
kind: Service
metadata:
  name: geopulse-backend
  namespace: geopulse
spec:
  type: ClusterIP
  selector:
    app: geopulse-backend
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
