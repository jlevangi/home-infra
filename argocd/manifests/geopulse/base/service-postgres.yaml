apiVersion: v1
kind: Service
metadata:
  name: geopulse-postgres
  namespace: geopulse
spec:
  type: ClusterIP
  selector:
    app: geopulse-postgres
  ports:
    - name: postgres
      port: 5432
      targetPort: postgres
      protocol: TCP
