apiVersion: v1
kind: LimitRange
metadata:
  name: headlamp-limits
  namespace: headlamp
spec:
  limits:
    - type: Container
      default:
        cpu: "500m"
        memory: 512Mi
      defaultRequest:
        cpu: 50m
        memory: 64Mi
