apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../base
images:
  - name: lscr.io/linuxserver/pairdrop
    newTag: "1.11.2"
patches:
  - target:
      kind: Ingress
      name: pairdrop-ingress
    patch: |
      - op: replace
        path: /spec/rules/0/host
        value: "pairdrop.stage.levangie.dev"
      - op: add
        path: /metadata/annotations/external-dns.alpha.kubernetes.io~1target
        value: "k3s-stage.levangie.dev"

commonLabels:
  environment: stage
