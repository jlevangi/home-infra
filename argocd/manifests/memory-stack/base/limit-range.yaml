apiVersion: v1
kind: LimitRange
metadata:
  name: memory-stack-limits
  namespace: memory-stack
spec:
  limits:
    - type: Container
      defaultRequest:
        cpu: 50m
        memory: 128Mi
      default:
        cpu: "1"
        memory: 512Mi
