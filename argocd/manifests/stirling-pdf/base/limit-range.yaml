apiVersion: v1
kind: LimitRange
metadata:
  name: stirling-pdf-storage-limits
  namespace: stirling-pdf
spec:
  limits:
    - type: PersistentVolumeClaim
      max:
        storage: "10Gi"
      min:
        storage: "1Gi"
