apiVersion: v1
kind: LimitRange
metadata:
  name: jellyfin-invite-limits
  namespace: jellyfin-invite
spec:
  limits:
    - type: Container
      default:
        cpu: 250m
        memory: 256Mi
      defaultRequest:
        cpu: 25m
        memory: 64Mi
