apiVersion: v1
kind: ResourceQuota
metadata:
  name: meshcentral-storage-quota
  namespace: meshcentral
spec:
  hard:
    requests.storage: "15Gi"
    persistentvolumeclaims: "5"
    longhorn.io/requests.storage: "15Gi"
