apiVersion: v1
kind: Service
metadata:
  name: meshcentral
  namespace: meshcentral
spec:
  selector:
    app: meshcentral
  ports:
    - name: http
      port: 80
      targetPort: 80
  type: ClusterIP
