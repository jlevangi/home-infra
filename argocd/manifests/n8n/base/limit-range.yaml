apiVersion: v1
kind: LimitRange
metadata:
  name: n8n-storage-limits
  namespace: n8n
spec:
  limits:
    - type: PersistentVolumeClaim
      max:
        storage: "25Gi"
      min:
        storage: "1Gi"
