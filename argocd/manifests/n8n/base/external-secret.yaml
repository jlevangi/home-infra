apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: n8n-secrets
  namespace: n8n
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: vault-kv
    kind: ClusterSecretStore
  target:
    name: n8n-secrets
    creationPolicy: Owner
  data:
    - secretKey: POSTGRES_PASSWORD
      remoteRef:
        key: prod/n8n
        property: POSTGRES_PASSWORD
    - secretKey: N8N_ENCRYPTION_KEY
      remoteRef:
        key: prod/n8n
        property: N8N_ENCRYPTION_KEY
    - secretKey: PAPERLESS_API_TOKEN
      remoteRef:
        key: prod/paperless
        property: API_TOKEN
    - secretKey: KEYCLOAK_INVITE_CLIENT_ID
      remoteRef:
        key: prod/n8n
        property: KEYCLOAK_INVITE_CLIENT_ID
    - secretKey: KEYCLOAK_INVITE_CLIENT_SECRET
      remoteRef:
        key: prod/n8n
        property: KEYCLOAK_INVITE_CLIENT_SECRET
    - secretKey: KEYCLOAK_INVITE_GROUP_ID
      remoteRef:
        key: prod/n8n
        property: KEYCLOAK_INVITE_GROUP_ID
    - secretKey: JELLYFIN_INVITE_CODES
      remoteRef:
        key: prod/n8n
        property: JELLYFIN_INVITE_CODES
    - secretKey: WIZARR_API_KEY
      remoteRef:
        key: prod/n8n
        property: WIZARR_API_KEY
