apiVersion: apps/v1
kind: Deployment
metadata:
  name: health-ingester
  namespace: health-archive
spec:
  replicas: 1
  selector:
    matchLabels:
      app: health-ingester
  template:
    metadata:
      labels:
        app: health-ingester
    spec:
      imagePullSecrets:
        - name: health-archive-ghcr
      containers:
        - name: ingester
          image: ghcr.io/jlevangi/health-ingester:0.4.2@sha256:eab5126e887f045216d686976af4e4fac43a3fa4542e34b9fa06da8bd86003d1
          imagePullPolicy: IfNotPresent
          env:
            - name: PGHOST
              value: health-postgres
            - name: PGDATABASE
              value: health_archive
            - name: PGUSER
              value: health_archive
            - name: PGPASSWORD
              valueFrom:
                secretKeyRef:
                  name: health-postgres-secret
                  key: postgres-password
            - name: HEALTH_COLLECTOR_TOKEN
              valueFrom:
                secretKeyRef:
                  name: health-collector-secret
                  key: collector-token
          ports:
            - name: http
              containerPort: 8080
          readinessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 15
            periodSeconds: 20
          resources:
            requests:
              cpu: 50m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
          volumeMounts:
            # waitress spills large request bodies to a temp file; with a
            # read-only root filesystem it has nowhere to write and drops the
            # connection mid-upload. Uploads here are whole export files, so
            # this path is hit by ordinary traffic, not just edge cases.
            - name: tmp
              mountPath: /tmp
      volumes:
        - name: tmp
          emptyDir:
            sizeLimit: 256Mi
