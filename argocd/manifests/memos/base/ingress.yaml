apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: memos-ingress
  namespace: memos
  annotations:
    traefik.ingress.kubernetes.io/router.entrypoints: web,websecure
spec:
  ingressClassName: traefik
  rules:
    - host: memos.levangie.org
      http:
        paths:
          - path: /api/v1
            pathType: Prefix
            backend:
              service:
                name: mortis
                port:
                  number: 5231
          - path: /o/r
            pathType: Prefix
            backend:
              service:
                name: mortis
                port:
                  number: 5231
          - path: /
            pathType: Prefix
            backend:
              service:
                name: memos
                port:
                  number: 5230
