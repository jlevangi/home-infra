apiVersion: v1
kind: ResourceQuota
metadata:
  name: memos-storage-quota
  namespace: memos
spec:
  hard:
    requests.storage: "1Gi"
    persistentvolumeclaims: "1"
    longhorn.io/requests.storage: "1Gi"
