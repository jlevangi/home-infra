apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: notemark-data-pvc
  namespace: notemark
  labels:
    app: notemark
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 500Mi
  storageClassName: longhorn
