apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: kioto-app-data-pvc
  namespace: kioto
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
  storageClassName: longhorn
  volumeName: kioto-app-data-pv
