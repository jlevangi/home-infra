apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: microbin-data-pvc
  namespace: microbin
  labels:
    app: microbin
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: longhorn
