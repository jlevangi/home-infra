apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: everlyera-secrets
  namespace: everlyera
spec:
  refreshInterval: 1h
  secretStoreRef: { name: vault-kv, kind: ClusterSecretStore }
  target: { name: everlyera-secrets, creationPolicy: Owner }
  dataFrom:
    - extract: { key: prod/everlyera }
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: everlyera-ghcr
  namespace: everlyera
spec:
  refreshInterval: 1h
  secretStoreRef: { name: vault-kv, kind: ClusterSecretStore }
  target:
    name: everlyera-ghcr
    creationPolicy: Owner
    template:
      type: kubernetes.io/dockerconfigjson
  data:
    - secretKey: .dockerconfigjson
      remoteRef: { key: prod/everlyera, property: GHCR_DOCKER_CONFIG_JSON }
