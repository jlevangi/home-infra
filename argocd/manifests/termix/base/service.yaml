apiVersion: v1
kind: Service
metadata:
  name: termix
  namespace: termix
  labels:
    app: termix
    app.kubernetes.io/name: termix
spec:
  type: ClusterIP
  selector:
    app: termix
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
