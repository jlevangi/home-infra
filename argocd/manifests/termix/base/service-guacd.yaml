apiVersion: v1
kind: Service
metadata:
  name: guacd
  namespace: termix
  labels:
    app: termix
    app.kubernetes.io/name: guacd
spec:
  type: ClusterIP
  selector:
    app: termix
  ports:
    - name: guacd
      port: 4822
      targetPort: 4822
      protocol: TCP
