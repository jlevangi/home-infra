apiVersion: v1
kind: ResourceQuota
metadata:
  name: healthchecks-storage-quota
  namespace: healthchecks
spec:
  hard:
    requests.storage: "1Gi"
    persistentvolumeclaims: "1"
    longhorn.io/requests.storage: "1Gi"
