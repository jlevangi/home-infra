apiVersion: v1
kind: Service
metadata:
  name: outline-redis
  namespace: outline
  annotations:
    argocd.argoproj.io/sync-wave: "-1"
spec:
  selector:
    app: outline-redis
  ports:
    - port: 6379
      targetPort: 6379
      name: redis
