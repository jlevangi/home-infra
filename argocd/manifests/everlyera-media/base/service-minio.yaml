apiVersion: v1
kind: Service
metadata:
  name: everlyera-minio
  namespace: everlyera-media
spec:
  selector: { app: everlyera-minio }
  ports:
    - { name: s3, port: 9000, targetPort: 9000 }
    - { name: console, port: 9001, targetPort: 9001 }
