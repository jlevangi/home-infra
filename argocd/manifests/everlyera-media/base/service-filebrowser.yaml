apiVersion: v1
kind: Service
metadata:
  name: everlyera-filebrowser
  namespace: everlyera-media
  labels:
    app: everlyera-filebrowser
spec:
  selector:
    app: everlyera-filebrowser
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
