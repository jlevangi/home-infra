apiVersion: v1
kind: Service
metadata:
  name: ytdl
  namespace: ytdl
  labels:
    app: ytdl
spec:
  type: ClusterIP
  selector:
    app: ytdl
  ports:
    - name: http
      port: 3033
      targetPort: 3033
      protocol: TCP
