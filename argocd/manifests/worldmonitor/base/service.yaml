apiVersion: v1
kind: Service
metadata:
  name: worldmonitor
  namespace: worldmonitor
  labels:
    app.kubernetes.io/name: worldmonitor
spec:
  selector:
    app.kubernetes.io/name: worldmonitor
  ports:
    - name: http
      port: 8080
      targetPort: http
