apiVersion: v1
kind: Service
metadata:
  name: firefox
  namespace: firefox
  labels:
    app: firefox
spec:
  type: ClusterIP
  selector:
    app: firefox
  ports:
    - name: firefox-http
      port: 3000
      targetPort: 3000
      protocol: TCP
