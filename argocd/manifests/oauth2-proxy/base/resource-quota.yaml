apiVersion: v1
kind: ResourceQuota
metadata:
  name: oauth2-proxy-quota
  namespace: oauth2-proxy
spec:
  hard:
    requests.cpu: "1"
    requests.memory: 1Gi
    limits.cpu: "2"
    limits.memory: 2Gi
