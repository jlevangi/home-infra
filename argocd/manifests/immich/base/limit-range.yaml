apiVersion: v1
kind: LimitRange
metadata:
  name: immich-storage-limits
  namespace: immich
spec:
  limits:
    - type: PersistentVolumeClaim
      max:
        storage: "20Gi"
      min:
        storage: "100Mi"
      default:
        storage: "1Gi"
      defaultRequest:
        storage: "1Gi"
