apiVersion: v1
kind: ResourceQuota
metadata:
  name: immich-storage-quota
  namespace: immich
spec:
  hard:
    requests.storage: "25Gi"
    persistentvolumeclaims: "5"
    longhorn.io/requests.storage: "25Gi"
