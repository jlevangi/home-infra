apiVersion: v1
kind: Service
metadata:
  name: netlock-server
  namespace: netlock-rmm
spec:
  selector:
    app: netlock-server
  ports:
    - name: http
      port: 7080
      targetPort: 7080
    - name: relay
      port: 7081
      targetPort: 7081
  type: ClusterIP
