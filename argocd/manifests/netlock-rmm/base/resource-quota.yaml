apiVersion: v1
kind: ResourceQuota
metadata:
  name: netlock-rmm-storage-quota
  namespace: netlock-rmm
spec:
  hard:
    requests.storage: "30Gi"
    persistentvolumeclaims: "10"
    longhorn.io/requests.storage: "30Gi"
