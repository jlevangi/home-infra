apiVersion: v1
kind: ResourceQuota
metadata:
  name: vaultwarden-storage-quota
  namespace: vaultwarden
spec:
  hard:
    requests.storage: "15Gi"
    persistentvolumeclaims: "10"
    longhorn.io/requests.storage: "15Gi"
