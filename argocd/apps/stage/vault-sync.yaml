apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: vault-sync
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "0"
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: git@github.com:jlevangi/home-infra.git
    targetRevision: stage
    path: argocd/manifests/vault-sync/overlays/stage
  destination:
    server: https://kubernetes.default.svc
    namespace: vault-raft
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
