apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: pairdrop
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: git@github.com:jlevangi/home-infra.git
    targetRevision: main
    path: argocd/manifests/pairdrop/overlays/test
  destination:
    server: https://kubernetes.default.svc
    namespace: pairdrop
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
